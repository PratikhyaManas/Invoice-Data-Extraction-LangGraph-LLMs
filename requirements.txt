# Runtime dependencies for the invoice extraction pipeline.
# Pin versions deliberately -- SCA scanning (pip-audit / safety) is run
# in CI against this exact set, see security/README.md.

langgraph==0.2.60
langchain-core==0.3.28
databricks-langchain==0.2.1
langchain-openai==0.2.14      # optional provider, only needed if llm.provider == "openai"

pdfplumber==0.11.4
pytesseract==0.3.13
Pillow==11.0.0

pandas==2.2.3

# Databricks / Spark are provided by the Databricks Runtime at job
# execution time; pinned here only so local dev environments and CI
# can install a matching version for tests that touch storage/schema.py.
pyspark==3.5.3
delta-spark==3.2.1
